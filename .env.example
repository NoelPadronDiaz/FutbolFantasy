# Copia este archivo como .env y rellena con los valores de tu proyecto
# de Neon/Vercel Postgres (Vercel > Storage > tu base de datos > .env.local).
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require
